# Javascript Node CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-javascript/ for more details
#
version: 2

jobs:
  build:
    working_directory: ~/repo
    docker:
      - image: circleci/node:12-browsers

    steps:
      - checkout

      # Download and cache dependencies
      - restore_cache:
          keys:
          - v1-dependencies-{{ checksum "package-lock.json" }}

      - run:
          name: Version Info
          command: |
            node --version \
              && yarn --version \
              && git --version
      - run: yarn install

      - save_cache:
          paths:
            - node_modules
          key: v1-dependencies-{{ checksum "package-lock.json" }}

      # Run unit tests!
      # - run: yarn test

      # Build affected apps and libraries
      - run: yarn affected:build

      # Store build artefacts on circle
      - store_artifacts:
          path: ~/repo/dist
          destination: dist
          
    dependencies:
      pre:
        - echo "scope=ramiz4" >> ~/.npmrc
        - echo "@ramiz4:registry=https://registry.npmjs.org/" >> ~/.npmrc
        - echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" >> ~/.npmrc
      
    deployment:
      npm:
        tag: /v[0-9]+(\.[0-9]+)*/
        commands:
          - npm publish dist/libs/ngx-multi-level-push-menu/
